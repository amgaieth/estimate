# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'estimate' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'GoogleMaps'
  pod 'SwiftyJSON'
  # Pods for estimate

  target 'estimateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'estimateUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
